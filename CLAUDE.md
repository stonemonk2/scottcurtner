# CLAUDE.md

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `stonemonk2/scottcurtner`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root, created lazily by `/domain-modeling`. See `docs/agents/domain.md`.
